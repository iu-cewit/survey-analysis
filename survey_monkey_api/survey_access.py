#Working with json objects generated by Survey Monkey API

def get_structure(json_object, nest=0):
    """Displays the structure of a json_object.

    json object -> none"""
    if not isinstance(json_object, list):
        if not isinstance(json_object, dict):
            print('    '*nest + str(json_object) + ' has type ' + \
                  str(type(json_object)))
        else:
            nest += 1
            for item in json_object.items():
                print('    '*nest + str(item[0]) + ' has type ' + \
                      str(type(item[1])))
                get_structure(item[1], nest)
    else:
        nest += 1
        for child in json_object:
            get_structure(child, nest)
    #this is giving me an extra layer for values rather than just keys

def list_respondents(json_object):
    """Returns a list of respondent ids from a dictionary of respondents

    json_object -> list of str"""
    ids = []
    for respondent in json_object['data']:
        ids.append(respondent['respondent_id'])
    return ids

##def get_questions(json_object):
##    """Returns a dictionary of questions and answers.
##
##    json object -> dict of str"""
    
def get_responses(json_object, question_id):
    """Returns a list of responses by respondent id for a given question id

    json_object, str -> list of dict"""
    
